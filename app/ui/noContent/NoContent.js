import React from "react";
import classes from "./noContent.module.css";
const NoContent = () => {
  return (
    <p className={classes["no-content"]}>you have not added any content yet </p>
  );
};

export default NoContent;
